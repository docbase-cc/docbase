{
  "name": "docbase",
  "version": "0.0.5",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build && bun ./postbuild.ts",
    "dev": "bun ./dev.ts"
  },
  "devDependencies": {
    "chalk": "^5.4.1"
  }
}
