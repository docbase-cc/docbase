{
  "name": "docbase",
  "version": "0.2.7",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --bun --filter '*' build && bun ./postbuild.ts",
    "dev": "bun run ./dev.ts"
  },
  "devDependencies": {
    "@terascope/fetch-github-release": "^2.1.0",
    "@types/bun": "^1.2.8",
    "chalk": "^5.4.1",
    "es-toolkit": "^1.34.1",
    "concurrently": "^9.1.2"
  }
}
