{
  "name": "docbase",
  "version": "0.0.7",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build && bun ./postbuild.ts",
    "dev": "bun ./dev.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.4",
    "chalk": "^5.4.1"
  }
}
