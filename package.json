{
  "name": "docbase",
  "version": "0.0.2",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "dev": "bun ./dev.ts"
  },
  "devDependencies": {
    "chalk": "^5.4.1"
  }
}
