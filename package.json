{
  "name": "docbase",
  "version": "0.0.27",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build && bun ./postbuild.ts",
    "dev": "bun run ./dev.ts"
  },
  "devDependencies": {
    "@terascope/fetch-github-release": "^2.0.0",
    "@types/bun": "^1.2.4",
    "chalk": "^5.4.1",
    "concurrently": "^9.1.2"
  }
}
